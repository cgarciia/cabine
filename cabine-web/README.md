# cabine-web

SPA da Cabine (totem + telas de operador). Fala com `cabine-core` via HTTP e WebSocket.

Mapa de pastas, rotas e regras: **[SPEC.md](./SPEC.md)**.

```bash
npm install
npm run dev
```

Vite em `http://127.0.0.1:5173` (proxy para a API em `:8000`).
